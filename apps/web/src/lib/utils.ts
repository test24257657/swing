/** Aceternity UI components import `cn` from `@/lib/utils` by convention. Keep this as the
 * canonical location and re-export from `@/lib/cn` so both import paths work. */
export { cn } from "./cn";
